server:
  port: 8100
spring:
  application:
    name: msvc-field

  data:
    mongodb:
      uri: mongodb://localhost:27017/Fielddatabase

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
