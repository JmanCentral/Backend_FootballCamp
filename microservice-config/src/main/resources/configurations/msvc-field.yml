server:
  port: 8100
spring:
  application:
    name: msvc-field
  data:
    mongodb:
      uri: mongodb://root:mongopass@localhost:27017/mydb?authSource=admin
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
